# =============================================================================
# .env.local — БД для разработки (локальный PostgreSQL)
# Скопируй: cp env.example.local .env.local
# Используется при APP_ENV=local (по умолчанию)
# =============================================================================

DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/run_app
